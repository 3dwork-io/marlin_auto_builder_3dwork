name: Debug Build (8-bit + 32-bit)
on:
  workflow_dispatch:
    inputs:
      force_version:
        description: Force version (e.g., 2.1.2.7)
        required: false
        default: ''
      version_dir:
        description: Version dir (e.g., 2.1.2.7)
        required: false
        default: ''

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Debug build ready"
